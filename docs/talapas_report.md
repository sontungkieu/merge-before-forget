# Talapas evidence report

Status: pending execution.

The exact job IDs, scheduler terminal states, hardware, runtime, artifacts, and
metrics are filled only after downloaded evidence has been verified. A Slurm
submission or `COMPLETED` state without a valid `training/summary.json` is not
treated as a scientific result.
